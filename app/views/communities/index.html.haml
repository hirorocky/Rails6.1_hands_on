%h1 掲示板
= link_to 'コミュニティ作成', new_community_path
- @communities.each do |community|
  %div
    %p= link_to community.title, community_path(community)
    %p= community.created_at
    %p= community.owner_name
